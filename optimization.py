from readfile import readFile, input2format


class Optimization()
    def __init__(self, N=0, server=[], M=0, virtualMachine=[], T=0, request=[]):
        self.N = N
        self.server = server
        self.M = M
        self.virtualMachine = virtualMachine
        self.T = T
        self.request = request
    def optimazationOneDay(self, AvailableServer=[],):

    
    